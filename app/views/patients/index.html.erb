<%= render 'shared/nav' %>
<div class="container-fluid">
    <%= render 'shared/sidenavbar'%>
<div class="row">
<div class="col-lg-8">
    <h1 class="tittle"> Todos los pacientes </h1>
      <table class="table">
      <thead class="thead-dark">
        <div class="col-md-6 col-md-offset-9">
        <%= form_tag(patients_path, method: 'get', id: 'search-form') do %>
          <%= text_field_tag :search, params[:search], placeholder: 'Nombre o numero paciente'%>
          <%= submit_tag 'Search', name: nil,class: "btn" %>
        <% end %>
        </div>

        <tr>
          <th scope="col">No. Paciente</th>
          <th scope="col">Nombre completo</th>
          <th scope="col">Telefóno</th>
          <th scope="col">Email</th>
          <th scope="col"></th>
          <th scope="col"></th>
          <th scope="col">

          </th>
        </tr>
      </thead>
        <tbody id="patients">
          <%= render @patients %>
        </tbody>
    </table>





  </div>
</div>
</div>
