<%= render 'shared/nav' %>
<div class="container-fluid">
    <%= render 'shared/sidenavbar'%>
  <div class="row">
    <h1 class="tittle"> <%= @patient.full_name %></h1>
    <div class="col-lg-8">
      <div class="well">
          <div>
      		    <h3><span>Número de paciente:</span> <%= @patient.id %></h3>
          </div>
          <div>
      		    <h3><span>Edad:</span> <%= @patient.age %></h3>
          </div>
          <div>
      		    <h3><span>Fecha de nacimiento:</span> <%= @patient.date_birth %></h3>
          </div>
      </div>
      <%= link_to 'Editar', edit_patient_path(@patient), class: 'btn btn-primary' %>
    </div>
  </div>
</div>
