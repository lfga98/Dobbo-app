<%= render 'shared/nav' %>
<div class="container-fluid">
    <%= render 'shared/sidenavbar'%>
  <div class="row">
    <h1 class="tittle"> Datos personales</h1>
    <div class="col-lg-8">
      <div class="well">
        <div>
            <h2><%= @patient.full_name %> </h2>
        </div>
        <hr>
          <div>
      		    <h4><span>Número de paciente:</span> <%= @patient.id %></h4>
          </div>
          <div>
      		    <h4><span>Edad:</span> <%= @patient.age %></h4>
          </div>
          <div>
      		    <h4><span>Fecha de nacimiento:</span> <%= @patient.date_birth %></h4>
          </div>
      </div>
      <%= link_to 'Editar', edit_patient_path(@patient), class: 'btn btn-primary' %>
    </div>
  </div>
</div>
